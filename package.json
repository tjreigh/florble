{
  "name": "florble",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=florble --branch=main --commit-dirty=true",
    "preview": "npm run build && wrangler pages dev dist",
    "start": "python3 -m http.server 8000",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.108.0"
  }
}
